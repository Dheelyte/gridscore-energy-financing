# Scripts

Developer and operational helpers.

- `seed_demo.py` — loads the curated, repeatable demo scenario (Stage 2),
  including the borderline customer who flips reject → approve under the pooled
  cooperative view.

> Stage 0 placeholder — scripts are added in the stages that need them.
